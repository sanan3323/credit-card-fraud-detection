# Runtime dependencies for the fraud scoring service (API + model).
# scikit-learn is pinned to the range that cleanly loads the shipped artifact.
fastapi==0.115.6
uvicorn[standard]==0.34.0
mangum==0.19.0
pydantic==2.10.4
numpy==2.0.2
pandas==2.2.3
scikit-learn==1.6.1
xgboost==2.1.3
shap==0.46.0
# Pin numba/llvmlite to wheel-having versions (shap -> numba -> llvmlite); avoids
# a source build that needs cmake.
numba==0.60.0
llvmlite==0.43.0
